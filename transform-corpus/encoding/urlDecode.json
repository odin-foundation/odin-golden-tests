{
  "id": "urlDecode",
  "family": "encoding",
  "title": "%urlDecode — Decode a percent-encoded string",
  "purpose": "Reverse percent-encoding, turning a URL component back into plain text.",
  "signature": "%urlDecode <string> -> string",
  "transform": "{out}\ndec = %urlDecode @.q\nplain = %urlDecode @.raw\nbad = %urlDecode @.malformed",
  "input": "q = \"a%20b%26c%3Dd\"\nraw = \"already-plain\"\nmalformed = \"%zz\"",
  "expectedOutput": "{out}\ndec = \"a b&c=d\"\nplain = \"already-plain\"\nbad = ~",
  "notes": [
    "Inverse of %urlEncode: %20 becomes a space, %26 an ampersand, and so on.",
    "A malformed escape (such as %zz) decodes to ~ (null) rather than raising an error."
  ],
  "avoid": [
    {
      "snippet": "dec = %urlDecode @.q   ; with q = \"%zz\"",
      "why": "an invalid percent escape yields ~, not partial output"
    }
  ]
}
