{
  "id": "pad",
  "family": "string",
  "title": "%pad - pad to a width (right side)",
  "purpose": "Pad the end of a string with a fill character until it reaches a target length.",
  "signature": "%pad <string> <length:integer> <char:string> -> string",
  "transform": "{out}\npadded = %pad @.code ##6 \".\"\nalready = %pad @.long ##3 \".\"\nexact = %pad @.exact ##2 \".\"",
  "input": "code = \"42\"\nlong = \"abcdef\"\nexact = \"ab\"",
  "expectedOutput": "{out}\npadded = \"42....\"\nalready = \"abcdef\"\nexact = \"ab\"",
  "notes": [
    "Pads on the right, behaving like %padRight.",
    "Three required arguments: string, target length, fill character; only the first character of the fill is used."
  ],
  "avoid": []
}
