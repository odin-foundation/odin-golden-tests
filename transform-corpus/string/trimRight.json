{
  "id": "trimRight",
  "family": "string",
  "title": "%trimRight — strip trailing whitespace",
  "purpose": "Remove whitespace from the end of a string only.",
  "signature": "%trimRight <string> -> string",
  "transform": "{out}\nout = %trimRight @.raw\ntrailingOnly = %trimRight @.back\nnoTrail = %trimRight @.clean",
  "input": "raw = \"  hello  \"\nback = \"abc   \"\nclean = \"  abc\"",
  "expectedOutput": "{out}\nout = \"  hello\"\ntrailingOnly = \"abc\"\nnoTrail = \"  abc\"",
  "notes": [
    "Only trailing whitespace is removed; leading whitespace is preserved.",
    "The argument must be a reference (@.raw) or a quoted literal."
  ],
  "avoid": [
    {
      "snippet": "out = %trimRight",
      "why": "missing the source argument — the engine raises a verb-arity error"
    }
  ]
}
