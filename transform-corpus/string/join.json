{
  "id": "join",
  "family": "string",
  "title": "%join — join array elements with a delimiter",
  "purpose": "Concatenate the elements of an array into a single string separated by a delimiter.",
  "signature": "%join <array> <delim:string> -> string",
  "transform": "{out}\nout = %join @.tags \", \"",
  "input": "tags[0] = \"a\"\ntags[1] = \"b\"\ntags[2] = \"c\"",
  "expectedOutput": "{out}\nout = \"a, b, c\"",
  "notes": [
    "First argument is an array reference; second is the quoted delimiter.",
    "An array source is supplied in ODIN with indexed assignments (tags[0], tags[1], ...)."
  ],
  "avoid": []
}
