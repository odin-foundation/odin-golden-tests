{
  "id": "replace",
  "family": "string",
  "title": "%replace - replace all literal occurrences",
  "purpose": "Replace every literal occurrence of a substring with a replacement.",
  "signature": "%replace <string> <find:string> <replacement:string> -> string",
  "transform": "{out}\nhit = %replace @.name \"o\" \"0\"\nmiss = %replace @.name \"z\" \"0\"\nall = %replace @.many \"a\" \"A\"",
  "input": "name = \"Honda\"\nmany = \"banana\"",
  "expectedOutput": "{out}\nhit = \"H0nda\"\nmiss = \"Honda\"\nall = \"bAnAnA\"",
  "notes": [
    "Three required arguments: string, literal find, literal replacement.",
    "Matching is literal (not regex) and replaces all occurrences. Use %replaceRegex for pattern matching."
  ],
  "avoid": [
    {
      "snippet": "out = %replace @.name \"o\"",
      "why": "missing the replacement argument - the engine raises a 3-argument arity error"
    }
  ]
}
