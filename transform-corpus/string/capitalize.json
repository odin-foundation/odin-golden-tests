{
  "id": "capitalize",
  "family": "string",
  "title": "%capitalize — capitalize the first letter",
  "purpose": "Uppercase the first character and lowercase the rest.",
  "signature": "%capitalize <string> -> string",
  "transform": "{out}\nout = %capitalize @.word",
  "input": "word = \"hONDA\"",
  "expectedOutput": "{out}\nout = \"Honda\"",
  "notes": [
    "First character is uppercased and every following character is lowercased.",
    "Operates on the whole string as one word — use %titleCase for per-word capitalization."
  ],
  "avoid": [
    { "snippet": "out = %capitalize honda", "why": "bare word honda is read as the literal string, capitalizing the literal instead of the field; use @.word" }
  ]
}
