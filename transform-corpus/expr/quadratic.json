{
  "id": "quadratic",
  "family": "expr",
  "title": "%expr — a quadratic formula",
  "purpose": "Combine variables, powers, and precedence in one formula with explicit bindings.",
  "signature": "%expr \"<formula>\" [@bindings] -> number",
  "transform": "{out}\nr = %expr \"a*x^2 + b*x + c\" @.v",
  "input": "{v}\na = ##2\nb = ##3\nc = ##4\nx = ##5",
  "expectedOutput": "{out}\nr = ##69",
  "notes": [
    "Each name reads the matching field of the bindings object @.v; this evaluates 2*25 + 3*5 + 4 = 69."
  ],
  "avoid": []
}
