{
  "id": "coerceDate",
  "family": "type",
  "title": "%coerceDate — convert a value to a date",
  "purpose": "Parse a string (or number/timestamp) into a typed date, dropping any time component.",
  "signature": "%coerceDate <value> -> date",
  "transform": "{out}\nd = %coerceDate @.raw",
  "input": "raw = \"2024-06-15\"",
  "expectedOutput": "{out}\nd = 2024-06-15",
  "notes": [
    "The quoted string becomes a bare date value (no quotes, no prefix) rendered as YYYY-MM-DD.",
    "Many input shapes parse: ISO, YYYYMMDD, MM/DD/YYYY, DD.MM.YYYY, written months, and numeric Unix timestamps.",
    "An unparseable value yields ~ (null) instead of an error."
  ],
  "avoid": [
    { "snippet": "d = %coerceDate @.raw  ; input \"not-a-date\"", "why": "an unrecognized date string silently becomes ~ (null), never an error" }
  ]
}
