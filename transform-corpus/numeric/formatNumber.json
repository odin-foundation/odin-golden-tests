{
  "id": "formatNumber",
  "family": "numeric",
  "title": "%formatNumber — fixed decimal-place string",
  "purpose": "Format a number as a string with a fixed number of decimal places.",
  "signature": "%formatNumber <number> <integer:decimals> -> string",
  "transform": "{out}\nv = %formatNumber @.a ##3",
  "input": "a = #3.14159",
  "expectedOutput": "{out}\nv = \"3.142\"",
  "notes": [
    "The result is a string (quoted), not a number — trailing/rounded decimals are fixed by toFixed.",
    "The decimals argument is required."
  ],
  "avoid": [
    { "snippet": "v = %formatNumber @.a", "why": "the decimals argument is required; omitting it raises a verb-arity error" }
  ]
}
