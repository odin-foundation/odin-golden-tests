{
  "id": "flatten",
  "family": "array",
  "title": "%flatten — flatten nested arrays one level",
  "purpose": "Collapse an array of arrays into a single array, one level deep.",
  "signature": "%flatten <array> -> array",
  "transform": "{out}\nall = %flatten @.nested\nflat = %flatten @.alreadyFlat",
  "input": "nested = \"[[1, 2], [3, 4]]\"\nalreadyFlat = \"[1, 2, 3]\"",
  "expectedOutput": "{out}\n{.all[] : ~}\n##1\n##2\n##3\n##4\n{.flat[] : ~}\n##1\n##2\n##3",
  "notes": [
    "A nested (2D) array is supplied as a JSON-array string literal; the engine parses it into nested arrays before flattening.",
    "Flattening is one level deep only — deeper nesting is preserved."
  ],
  "avoid": []
}
