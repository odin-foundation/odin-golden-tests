{
  "id": "parseDate",
  "family": "date",
  "title": "%parseDate — parse a string into an ISO date",
  "purpose": "Read a date from a formatted string and normalize it to YYYY-MM-DD.",
  "signature": "%parseDate <string:value> <string:pattern> -> string",
  "transform": "{out}\nd = %parseDate @.raw \"MM/DD/YYYY\"",
  "input": "raw = \"06/15/2024\"",
  "expectedOutput": "{out}\nd = \"2024-06-15\"",
  "notes": [
    "The pattern locates each component by position (MM, DD, YYYY).",
    "Output is always an ISO date string regardless of the input pattern."
  ],
  "avoid": [
    { "snippet": "d = %parseDate @.raw", "why": "the pattern argument is required; omitting it raises a verb-arity error" }
  ]
}
