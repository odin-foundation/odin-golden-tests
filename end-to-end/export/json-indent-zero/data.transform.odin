{$}
odin = "1.0.0"
transform = "1.0.0"
direction = "odin->json"
target.format = "json"
target.indent = ##0
description = "Export ODIN to minified JSON (no indentation)"

{user}
name = @user.name
age = @user.age
active = @user.active
