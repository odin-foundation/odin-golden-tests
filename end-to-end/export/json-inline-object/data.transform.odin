{$}
odin = "1.0.0"
transform = "1.0.0"
direction = "odin->json"
target.format = "json"
description = ":object builds a nested object from an inline spec"

{Quote}
contact = ":object {name = @insured.name, phone = @insured.phone, email = @insured.email}"
